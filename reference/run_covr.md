# Run covr in subprocess with timeout

Run covr in subprocess with timeout

## Usage

``` r
run_covr(path, timeout)
```

## Arguments

- path:

  \- path to source file

- timeout:

  \- length of timeout - set to Inf
